单例设计模式：经过无数次尝试，总结出来一套最佳实践。


单例：一个类在内存中只有一个对象，别人不能再去创建对象。


使用场景；工具类（节省资源）


写法

1.必须在该类中创建一个对象
2.私有化的构造器，防止外界通过构造器来创建新的对象。
3.给外界提供一个方法能够获取已经创建好的对象


在一个类里面创建一个对象（私有化自己 private  名  1 = new 名()  ）
私有化自己的构造器
（同上）
.给外界提供一个方法能够获取已经创建好的对象
public 类名 随便（）{
return 1;
}