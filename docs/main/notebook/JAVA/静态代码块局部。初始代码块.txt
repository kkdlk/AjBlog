代码块当中定义的变量，我们称它为局部代码块

局部变量（只能在他的大括号“{}”内使用）
全局变量（能在类中任意地方使用的变量）
static  静态（表明它属于整个类）
1.局部代码块
	直接定义在方法内部的代码块
	再调用方法的时候执行

2.初始代码块
	直接定义在类中
	初始化代码块在构造方法中执行（每创建一个对象就执行一次）
	
3.静态代码块
	初始化代码块前加上一个 static （优先执行）
	在加载成字节码的时候执行，在主方法前执行，只执行一次
	
	
	